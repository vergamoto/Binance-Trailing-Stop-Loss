version: '3'
services:
  py:
    build:
      context: ./app
    volumes:
      - ./app:/app
      - ./.dev-local/home:/root
    working_dir: /app
    env_file:
      - ./.dev-local/home/.env
    command: bash

